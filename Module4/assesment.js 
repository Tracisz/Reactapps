import React, {useContext} from "react"


function App() { 
    [count, setCount] = react.useContext()

    function setCount(){ 
        return(count + 1)
        
    }
    return( 
        <button onChange = {setCount}></button>
    )
}

export default App 